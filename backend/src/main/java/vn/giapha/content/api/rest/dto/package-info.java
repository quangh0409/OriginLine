/**
 * DTO của hợp đồng REST {@code /api/v1/posts} và {@code /api/v1/honours}.
 *
 * <p>Hình dạng do chủ dự án chốt và frontend đang xây theo — <b>đổi là phá client đang chạy</b>.
 * Tách khỏi view của tầng application để hợp đồng HTTP và use case tiến hoá độc lập.</p>
 */
package vn.giapha.content.api.rest.dto;
