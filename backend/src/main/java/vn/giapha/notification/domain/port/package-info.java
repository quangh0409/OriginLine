/**
 * <b>Port</b> do domain của {@code notification} khai báo.
 *
 * <p>Quan trọng nhất là {@code NotificationProvider}: thêm một kênh gửi (Zalo ZNS ở Giai đoạn 2) chỉ
 * là thêm một lớp hiện thực giao diện này, không phải sửa luồng gửi.</p>
 */
package vn.giapha.notification.domain.port;
