/**
 * DTO trên dây của context {@code notification}, khớp {@code contracts/openapi.yaml}.
 *
 * <p>{@code NotificationCategoryApiMapper} ghi lại chỗ lệch giữa hợp đồng và ràng buộc CHECK của
 * migration — đọc javadoc của lớp đó trước khi thêm loại thông báo mới.</p>
 */
package vn.giapha.notification.api.rest.dto;
