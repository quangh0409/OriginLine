/**
 * REST controller của context {@code notification}: trung tâm thông báo in-app và đăng ký Web Push.
 *
 * <p>Mọi endpoint ở đây thao tác trên dữ liệu của <b>chính người đang đăng nhập</b>, suy từ JWT.
 * Không có và sẽ không có tham số {@code userId}.</p>
 */
package vn.giapha.notification.api.rest;
