/**
 * Kiểu trả về của tầng {@code application} context {@code media}.
 *
 * <p>Chỉ {@link vn.giapha.media.application.view.MediaAssetView} mang nhãn
 * {@code @NamedInterface("gan-tep")} — đó là kiểu duy nhất ở gói này mà context khác nhìn thấy.
 * Ba kiểu còn lại phục vụ tầng {@code api} của chính context này.</p>
 */
package vn.giapha.media.application.view;
