/**
 * Command của tầng application context {@code membership}: dữ liệu vào của use case, tách khỏi DTO
 * của HTTP để tầng api đổi hình dạng mà không kéo theo tầng dưới.
 */
package vn.giapha.membership.application.command;
