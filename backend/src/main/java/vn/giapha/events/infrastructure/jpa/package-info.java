/**
 * Entity JPA và adapter Spring Data cho bảng {@code event}.
 *
 * <p>Đây là nơi <b>duy nhất</b> trong context {@code events} được mang annotation JPA. Bảng
 * {@code reminder_job} cố ý không có entity — xem
 * {@code events.infrastructure.jdbc.ReminderJobJdbcRepository}.</p>
 */
package vn.giapha.events.infrastructure.jpa;
